// DEPRECATED — KPI 시대 trees. redesign 의 alignedOrgGoalId 와는 다름.
import type { Goal } from '@/features/goals/model/types';

export function goalDepthInList(_goal: Goal, _goalsInList: Goal[]): number {
  return 0;
}
